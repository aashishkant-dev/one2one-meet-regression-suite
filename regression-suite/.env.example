# Copy this file to .env and fill in real values. .env is gitignored — never commit real credentials.
# Source for every value below: KNOWLEDGE.md (../../KNOWLEDGE.md) unless marked LOST.

# --- Environments ---
O2O_STAGING_BASE_URL=https://one2one.techarttrekkies.com.np
O2O_STAGING_API_BASE=https://one2one-backend.techarttrekkies.com.np
O2O_MAILPIT_BASE=http://121mail.techarttrekkies.com.np

# --- Super Admin (staging) ---
O2O_SUPERADMIN_USERNAME=
O2O_SUPERADMIN_PASSWORD=

# --- QA E2E Organizer (staging test org, "QA E2E Org 1786259627695") ---
O2O_ORG_USERNAME=
O2O_ORG_PASSWORD=

# --- Booking Test Event (id 362, slug booking-test-event-6a78985de09a0) ---
# This is the required fixture for Meeting Booking, Manual Booking, Meeting Request
# Lifecycle, Notifications/UX, and Concurrency specs (booking window open 03 Aug-05 Sep 2026).
O2O_BOOKING_EVENT_SLUG=booking-test-event-6a78985de09a0
# LOST: delegate passwords for BTE-BDC-alex / BTE-BEC-blake were never captured outside the
# original session's .env. Re-derive via Mailpit search (see README) or re-create the two
# delegates + Activate All on this event, which will re-dispatch credentials to Mailpit.
O2O_BOOKING_DELEGATE_A_USERNAME=BTE-BDC-alex
O2O_BOOKING_DELEGATE_A_PASSWORD=
O2O_BOOKING_DELEGATE_B_USERNAME=BTE-BEC-blake
O2O_BOOKING_DELEGATE_B_PASSWORD=

# --- Current Event (id 360, slug current-event-6a782b73dc037) sample delegates ---
# Used for CRUD/config specs that don't need an open booking window.
O2O_CURRENT_EVENT_SLUG=current-event-6a782b73dc037
O2O_CURRENT_DELEGATE_USERNAME=CE-OV-riley-12
O2O_CURRENT_DELEGATE_PASSWORD=

# --- Past Event (id 359, slug past-event-6a782b11646d6) ---
# Used for pagination (20 delegates) and read-only Reports/Live-Meetings specs.
O2O_PAST_EVENT_SLUG=past-event-6a782b11646d6

# --- Production (read-only smoke only; do not run mutating specs against this) ---
O2O_PROD_BASE_URL=https://app.one2onemeet.com
O2O_PROD_ORG_USERNAME=
O2O_PROD_ORG_PASSWORD=
